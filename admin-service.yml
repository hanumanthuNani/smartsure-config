spring:
  datasource:
    url: jdbc:mysql://localhost:3306/admin_db
    username: root
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  cache:
    type: simple
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    listener:
      simple:
        auto-startup: true

feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000

claims:
  service:
    url: http://localhost:8083

policy:
  service:
    url: http://localhost:8082
